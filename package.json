{
  "scripts": {
    "prepare": "rm -rf .next && cp -r /c/dev/2021-xps/json-business/site/.next .",
    "sls:deploy": "sls deploy",
    "sls:logs": "sls logs -f anyPath -t"
  },
  "devDependencies": {
    "serverless-bundle": "^5.3.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "mime-types": "^2.1.35"
  }
}
